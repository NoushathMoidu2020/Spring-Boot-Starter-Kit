CREATE TABLE EMPLOYEE
(
    id               INT generated by default as identity PRIMARY KEY,
    EMPLOYEE_NAME    VARCHAR(255),
    EMPLOYEE_SALARY  INT,
    DEPARTMENT       VARCHAR(255),
    PROFILE_PIC_PATH VARCHAR(225)
);
